-- /home/fordrl/e/lean4internals/books/lean4core/Vtd_src/Vtd_lake/Vtd_schemas.lean
        


import VersoManual
-- This gets access to most of the manual genre (which is also useful for textbooks)
open Verso.Genre Manual

-- This gets access to Lean code that's in code blocks, elaborated in the same process and
-- environment as Verso
open Verso.Genre.Manual.InlineLean

#doc (Manual) "`src/lake/schemas`"  =>

%%%
authors := ["Richard L Ford"]
tag := "src/lake/schemas"
%%%

TODO


# Files in `src/lake/schemas`
%%%
tag := "src/lake/schemas-files"
%%%

: `lakefile-toml-schema.json`

  [source](vscode:/home/fordrl/e/lean4internals/mods/lean4/src/lake/schemas/lakefile-toml-schema.json)TODO

