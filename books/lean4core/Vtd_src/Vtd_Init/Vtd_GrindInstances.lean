-- /home/fordrl/e/lean4internals/books/lean4core/Vtd_src/Vtd_Init/Vtd_GrindInstances.lean
        

-- Imports from child directories.

import «Vtd_src».«Vtd_Init».«Vtd_GrindInstances».«Vtd_Ring»

-- End of Imports from child directories.



import VersoManual
-- This gets access to most of the manual genre (which is also useful for textbooks)
open Verso.Genre Manual

-- This gets access to Lean code that's in code blocks, elaborated in the same process and
-- environment as Verso
open Verso.Genre.Manual.InlineLean

#doc (Manual) "`src/Init/GrindInstances`"  =>

%%%
authors := ["Richard L Ford"]
tag := "src/Init/GrindInstances"
%%%

TODO


{include «Vtd_src».«Vtd_Init».«Vtd_GrindInstances».«Vtd_Ring»}

# Files in `src/Init/GrindInstances`
%%%
tag := "src/Init/GrindInstances-files"
%%%

: `Nat.lean`

  [source](vscode:/home/fordrl/e/lean4internals/mods/lean4/src/Init/GrindInstances/Nat.lean)TODO

: `Ring.lean`

  [source](vscode:/home/fordrl/e/lean4internals/mods/lean4/src/Init/GrindInstances/Ring.lean)TODO

: `ToInt.lean`

  [source](vscode:/home/fordrl/e/lean4internals/mods/lean4/src/Init/GrindInstances/ToInt.lean)TODO

