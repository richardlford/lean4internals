-- /home/fordrl/e/lean4internals/books/lean4core/Vtd_src/Vtd_Init/Vtd_Internal.lean
        

-- Imports from child directories.

import «Vtd_src».«Vtd_Init».«Vtd_Internal».«Vtd_Order»

-- End of Imports from child directories.



import VersoManual
-- This gets access to most of the manual genre (which is also useful for textbooks)
open Verso.Genre Manual

-- This gets access to Lean code that's in code blocks, elaborated in the same process and
-- environment as Verso
open Verso.Genre.Manual.InlineLean

#doc (Manual) "`src/Init/Internal`"  =>

%%%
authors := ["Richard L Ford"]
tag := "src/Init/Internal"
%%%

TODO


{include «Vtd_src».«Vtd_Init».«Vtd_Internal».«Vtd_Order»}

# Files in `src/Init/Internal`
%%%
tag := "src/Init/Internal-files"
%%%

: `Order.lean`

  [source](vscode:/home/fordrl/e/lean4internals/mods/lean4/src/Init/Internal/Order.lean)TODO

